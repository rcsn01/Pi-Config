adjust subagent, plan mode, to support "default" rather than specific model
adjust /context to include token, cache token from subagents etc
adjust adjustable guardian model

adjust up arrow to show ctrl + c messages
adjust esc esc to show better re-roll

- Per-profile model cycling — cycle only within the normal profile's list vs plan profile's list
- Per-profile model cycling — cycle only within the normal profile's list vs plan profile's list
- - Per-profile model cycling — cycle only within the normal profile's list vs plan profile's list
- also fix auto compact
- Auto-name sessions from the first prompt — instead of session-2026-… ids
- - Guardian allow-list — e.g. always allow pnpm test/pnpm typecheck without prompting (you run
  these constantly)
