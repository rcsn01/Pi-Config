adjust subagent, plan mode, to support "default" rather than specific model
adjust adjustable guardian model

adjust esc esc to show better re-roll

Guardian allow-list — e.g. always allow pnpm test/pnpm typecheck without prompting (you run
  these constantly)

- check out what/session is about
''
make /profile session independent instead of relying on /settings
